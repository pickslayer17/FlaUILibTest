# Working agreement

When implementing a requested change, modify only the explicitly requested code and the minimum directly dependent call sites needed for it to compile. Do not make opportunistic refactors, cleanup, architectural changes, behavior changes, or edits to adjacent code unless the user explicitly asks for each of them.

If a possible issue is noticed outside the requested scope, report it separately and do not edit it.
